#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include "telas.h"
#include "validadores.h"
#include "pessoas.h"
#include "inserir_dados.h"
#include "produtos.h"

/*
void legenda_produtos(void)
{
	system("clear||cls");
	printf("*******************************************************************************\n");
	printf("***             = = = = = Menu de cadastro de produtos = = = = =             ***\n");
	printf("*******************************************************************************\n");
}

Produto* cadastro_produtos(struct Pilha* pilha)
{
	Produto *produto = malloc(sizeof(Produto));

	int codigo_v, quantidade_v, status_v = 1, tipo = 2, controle = 0;
	double preco_v;
	char nome_v[100], tipo_v[50]data_v[11];


	while (controle != 3)
	{
		legenda_produtos();

		if(bool recebe_preco(&preco_v))
		{
			legenda_produtos();
		}

	}
	return produto;
}




*/









/* if (funcionario != NULL)
{
	funcionario->status = status_v;
	funcionario->idade = idade_v;
	funcionario->salario = salario_v;
	strncpy(funcionario->id, id_v, 15);
	strncpy(funcionario->nome, nome_v, 100);
	strncpy(funcionario->cargo, cargo_v, 100);
	strncpy(funcionario->endereco, endereco_v, 150);
	strncpy(funcionario->email, email_v, 150);
	strncpy(funcionario->telefone, telefone_v, 20);
	strncpy(funcionario->cpf, cpf_v, 12);
	strncpy(funcionario->data, data_v, 11);



	printf("Produto cadastrado com sucesso\n");
	system("pause");
	system("clear||cls");
	controle = 3;
	desempilhar(pilha);
}
else
{
	printf("Falha na alocação de memória\n");
}
*/
